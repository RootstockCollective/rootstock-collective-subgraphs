{
  "name": "rootstock-collective-subgraphs",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "prepare:dev": "mustache dev.json subgraph.template.yaml > subgraph.yaml",
    "prepare:dao-qa": "mustache dao.qa.json subgraph.template.yaml > subgraph.yaml",
    "prepare:cr-qa": "mustache cr.qa.json subgraph.template.yaml > subgraph.yaml",
    "prepare:testnet": "mustache testnet.json subgraph.template.yaml > subgraph.yaml",
    "prepare:release-candidate-testnet": "mustache release-candidate-testnet.json subgraph.template.yaml > subgraph.yaml",
    "prepare:release-candidate-mainnet": "mustache release-candidate-mainnet.json subgraph.template.yaml > subgraph.yaml",
    "prepare:mainnet": "mustache mainnet.json subgraph.template.yaml > subgraph.yaml",
    "deploy:dev": "npm run prepare:dev && graph deploy --node https://api.studio.thegraph.com/deploy/ rootstockcollective-collective-rewards-dev",
    "deploy:dao-qa": "npm run prepare:dao-qa && graph deploy --node https://api.studio.thegraph.com/deploy/ rootstockcollective-collective-rewards-dao-qa",
    "deploy:cr-qa": "npm run prepare:cr-qa && graph deploy --node https://api.studio.thegraph.com/deploy/ rootstockcollective-collective-rewards-cr-qa",
    "deploy:testnet": "npm run prepare:testnet && graph deploy --node https://api.studio.thegraph.com/deploy/ rootstockcollective-collective-rewards-testnet",
    "deploy:release-candidate-testnet": "npm run prepare:release-candidate-testnet && graph deploy --node https://api.studio.thegraph.com/deploy/ rootstockcollective-collective-rewards-release-candidate-testnet",
    "deploy:release-candidate-mainnet": "npm run prepare:release-candidate-mainnet && graph deploy --node https://api.studio.thegraph.com/deploy/ rootstockcollective-collective-rewards-release-candidate-mainnet",
    "deploy:mainnet": "npm run prepare:mainnet && graph deploy --node https://api.studio.thegraph.com/deploy/ rootstockcollective-collective-rewards",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0",
    "mustache": "^4.2.0"
  }
}
